         UIApplication.shared.open(URL(string: "https://itunes.apple.com/app/id\(Constants.ITUNE_APP_ID)?action=write-review")!, options: [:], completionHandler: nil)
                 
                }
                
            }
            else {
                self.         UIApplication.shared.open(URL(string: "https://itunes.apple.com/app/id\(Constants.ITUNE_APP_ID)?action=write-review")!, options: [:], completionHandler: nil)
                 
                }
                
            }
            else {
                self.         UIApplication.shared.open(URL(string: "https://itunes.apple.com/app/id\(Constants.ITUNE_APP_ID)?action=write-review")!, options: [:], completionHandler: nil)
                 
                }
                
            }
            else {
                self.         UIApplication.shared.open(URL(string: "https://itunes.apple.com/app/id\(Constants.ITUNE_APP_ID)?action=write-review")!, options: [:], completionHandler: nil)
                 
                }
                
            }
            else {
                self.         UIApplication.shared.open(URL(string: "https://itunes.apple.com/app/id\(Constants.ITUNE_APP_ID)?action=write-review")!, options: [:], completionHandler: nil)
                 
                }
                
            }
            else {
                self.         UIApplication.shared.open(URL(string: "https://itunes.apple.com/app/id\(Constants.ITUNE_APP_ID)?action=write-review")!, options: [:], completionHandler: nil)
                 
                }
                
            }
            else {
                self.
